///<reference path='../cocos2d.d.ts'/>
///<reference path='Sprite_Character.ts'/>

/**
 * Game_Player
 *
 * The only one instance in GameScene
 *
 */
class Game_Player extends Sprite_Character {
    /**
    * Initialize all layers
    */
    constructor(root) {
        super(root);
    }

    public update(dt:number) {
        super.update(dt);
    }
}
